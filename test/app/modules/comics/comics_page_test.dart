import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:marvel_api/app/modules/comics/comics_page.dart';

main() {
  testWidgets('ComicsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ComicsPage(title: 'Comics')));
    //  final titleFinder = find.text('Comics');
    //  expect(titleFinder, findsOneWidget);
  });
}
